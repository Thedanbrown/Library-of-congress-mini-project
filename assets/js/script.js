// Get the search form

/**
 * Gets input and select values
 * If no input, returns and consoles an error message
 * Concatenates a string to send the user to the search results page
 *  with query parameters that it can use
 * Redirects the user to the url constructed above
 * @param {*} event
 */
 function handleSearchFormSubmit(event) {}

 // Add event listener for search form
 